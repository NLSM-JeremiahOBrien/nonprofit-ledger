---
phase: 02-auth-rbac-backup-dr-foundations
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - server/users/users.go
  - server/users/users_test.go
  - server/api/users.go
  - server/api/users_test.go
  - server/api/routes.go
  - cmd/server/main.go
autonomous: true
requirements: [AUTH-02, AUTH-03]

must_haves:
  truths:
    - "Admin can create a user account with any of the three roles (staff_bookkeeper, admin, external_accountant)"
    - "Creating a user with an invalid role is rejected both at the Go layer and by the SQL CHECK constraint"
    - "A non-admin (staff_bookkeeper or external_accountant) gets 403 attempting any user-management endpoint"
    - "External accountant can reach ledger read/write and report routes but gets 403 on user-management and org-settings routes"
  artifacts:
    - path: "server/users/users.go"
      provides: "Full user CRUD: Create, List, ByID, ByUsername, UpdateRole, Deactivate"
      exports: ["CreateUser", "ListUsers", "UpdateRole", "Deactivate"]
    - path: "server/api/users.go"
      provides: "Admin-only REST handlers for user management"
    - path: "server/api/routes.go"
      provides: "Explicit route-group wiring: admin-only group vs all-authenticated-roles group"
  key_links:
    - from: "server/api/routes.go"
      to: "server/auth/middleware.go"
      via: "RequireRole(\"admin\") wraps /api/users/* ; RequireRole(\"admin\",\"staff_bookkeeper\",\"external_accountant\") wraps ledger/report routes"
      pattern: "RequireRole"
    - from: "server/api/users.go"
      to: "server/users/users.go"
      via: "handler calls CreateUser/ListUsers/UpdateRole"
      pattern: "CreateUser|ListUsers|UpdateRole"
---

<objective>
Complete user management (AUTH-02) and enforce the external accountant's narrower scope (AUTH-03) via fail-closed route-group middleware, building directly on the auth/session foundation from plan 02-01.

Purpose: An admin needs a real way to create/manage the three fixed roles, and the external-accountant role's boundary (ledger/reports yes, user-mgmt/settings no) must be enforced centrally, not scattered across handlers.
Output: Full `server/users` CRUD, admin-only `/api/users/*` endpoints, and an explicit route-group file proving the external-accountant boundary.
</objective>

<execution_context>
@/Users/darren/.claude/get-shit-done/workflows/execute-plan.md
@/Users/darren/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-auth-rbac-backup-dr-foundations/02-RESEARCH.md
@.planning/phases/02-auth-rbac-backup-dr-foundations/02-VALIDATION.md
@server/users/users.go
@server/auth/middleware.go
@cmd/server/main.go

<interfaces>
<!-- Built in 02-01, this plan extends/consumes them directly. -->

From server/users/users.go (02-01 minimal surface — extend, do not replace):
```go
type User struct { ID int64; Username string; PasswordHash string; Role string; Active bool; MustChangePassword bool }
func ByUsername(db *sql.DB, username string) (*User, error)
func ByID(db *sql.DB, id int64) (*User, error)
```

From server/auth/middleware.go (02-01):
```go
type CurrentUser struct { ID int64; Role string }
func RequireAuth(next http.Handler) http.Handler
```
This plan adds `RequireRole(allowed ...string) func(http.Handler) http.Handler` per the research pattern — reads `CurrentUser` from context (set by `RequireAuth`), 403s if role not in `allowed`. Fixed role enum, exactly matching the SQL CHECK from migration 0005: `staff_bookkeeper`, `admin`, `external_accountant`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: User CRUD with role validation</name>
  <files>server/users/users.go, server/users/users_test.go</files>
  <behavior>
    - TestCreateUser: creating a user with each of the three valid roles (staff_bookkeeper, admin, external_accountant) succeeds and returns a user with a hashed (never plaintext) password stored. Creating a user with an invalid role string (e.g. "superuser") is rejected by Go-side validation before it ever reaches SQL, AND a direct SQL insert bypassing Go validation is independently rejected by the migration 0005 CHECK constraint (confirm both layers). Creating a user with a duplicate username fails cleanly.
  </behavior>
  <action>
    Extend `server/users/users.go` with: `CreateUser(db, username, password, role string) (*User, error)` — validates `role` against the fixed Go-side enum (mirroring the SQL CHECK exactly: `staff_bookkeeper`, `admin`, `external_accountant`) before calling `auth.HashPassword` and inserting; `ListUsers(db) ([]User, error)`; `UpdateRole(db, userID int64, newRole string) error` (same role validation); `Deactivate(db, userID int64) error` (sets `active = 0`, does not delete the row). Never store or return plaintext passwords in any struct/response.
  </action>
  <verify>
    <automated>go test ./server/users/... -run TestCreateUser -x</automated>
  </verify>
  <done>All three roles create successfully; invalid role rejected at both Go and SQL layers; duplicate username rejected; TestCreateUser green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RequireRole middleware + admin-only user-management handlers</name>
  <files>server/auth/middleware.go, server/api/users.go, server/api/users_test.go</files>
  <behavior>
    - TestUserMgmtRequiresAdmin: a logged-in `staff_bookkeeper` or `external_accountant` gets 403 on every `/api/users/*` route (list, create, update-role, deactivate); a logged-in `admin` gets the expected 2xx on each.
  </behavior>
  <action>
    Add `RequireRole(allowed ...string) func(http.Handler) http.Handler` to `server/auth/middleware.go`: builds a set from `allowed`, reads `CurrentUser` from context (set by `RequireAuth`, which must run first in the chain), returns 403 if the role isn't in the set. This is generic — plan 03/04 admin-only routes reuse it directly, no per-route custom checks.

    Build `server/api/users.go`: `GET /api/users` (list), `POST /api/users` (create), `PATCH /api/users/{id}/role` (update role), `POST /api/users/{id}/deactivate`. Wire all four behind `RequireAuth` then `RequireRole("admin")` — admin-only, no other role gets access to user management, ever.
  </action>
  <verify>
    <automated>go test ./server/auth/... -run TestUserMgmtRequiresAdmin -x</automated>
  </verify>
  <done>Non-admin roles receive 403 on all user-management routes; admin succeeds on all of them; TestUserMgmtRequiresAdmin green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Explicit route groups enforcing external-accountant scope</name>
  <files>server/api/routes.go, cmd/server/main.go</files>
  <behavior>
    - TestExternalAccountantScope: a logged-in `external_accountant` gets 403 on `/api/users/*` and any org-settings route (e.g. period lock), but gets a normal (non-403) response on ledger read/write and report routes.
  </behavior>
  <action>
    Create `server/api/routes.go` as the single place route groups are defined, per the research fail-closed pattern (never scatter per-handler `if role != ...` checks): an `adminOnlyGroup` (user management, org/period-lock settings) wrapped in `RequireRole("admin")`, and an `allRolesGroup` (ledger CRUD, balance/report reads) wrapped in `RequireRole("admin", "staff_bookkeeper", "external_accountant")`. External accountant is deliberately never added to `adminOnlyGroup` — the boundary is enforced by omission from that group, not a special-cased denial. Move the existing `/api/users/*` and ledger-related route registration out of `cmd/server/main.go` into this file's `RegisterRoutes(mux *http.ServeMux, ...)` function, called once from `main()`.
  </action>
  <verify>
    <automated>go test ./server/auth/... -run TestExternalAccountantScope -x</automated>
  </verify>
  <done>External accountant is blocked (403) from user-mgmt/settings routes and allowed on ledger/report routes; TestExternalAccountantScope green; `go build ./...` succeeds.</done>
</task>

</tasks>

<verification>
`go test ./server/users/... ./server/auth/... ./server/api/... -x` passes in full; `go build ./...` succeeds; `go test ./...` (full suite, including Phase 1 packages) remains green.
</verification>

<success_criteria>
AUTH-02 (admin creates/manages users across the three fixed roles) and AUTH-03 (external accountant scoped narrower than admin, enforced centrally and fail-closed) are fully implemented and test-covered.
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth-rbac-backup-dr-foundations/02-02-SUMMARY.md`
</output>
