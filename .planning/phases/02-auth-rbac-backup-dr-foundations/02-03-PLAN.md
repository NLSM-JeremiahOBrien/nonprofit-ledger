---
phase: 02-auth-rbac-backup-dr-foundations
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - server/db/migrations/0007_audit_log.up.sql
  - server/db/migrations/0007_audit_log.down.sql
  - server/audit/audit.go
  - server/audit/audit_test.go
  - server/ledger/posting.go
  - server/ledger/reversal.go
  - server/ledger/period.go
autonomous: true
requirements: [AUTH-05]

must_haves:
  truths:
    - "Posting a new journal entry writes an audit_log row attributing the action to the posting user"
    - "Reversing a journal entry writes an audit_log row attributing the correction to the reversing user"
    - "Locking an accounting period writes an audit_log row attributing the settings change to the locking user"
    - "Attempting to UPDATE or DELETE any audit_log row is rejected by the database, not just application logic"
    - "The actor recorded in an audit row always comes from server-side caller identity, never from unauthenticated client input"
  artifacts:
    - path: "server/db/migrations/0007_audit_log.up.sql"
      provides: "append-only audit_log table with BEFORE UPDATE/DELETE RAISE(ABORT) triggers"
      contains: "audit_log"
    - path: "server/audit/audit.go"
      provides: "Write(tx, actorUserID, action, entityType, entityID, detail) helper"
      exports: ["Write"]
  key_links:
    - from: "server/ledger/posting.go"
      to: "server/audit/audit.go"
      via: "audit.Write call inside PostJournalEntry's existing transaction, before commit"
      pattern: "audit\\.Write"
    - from: "server/ledger/reversal.go"
      to: "server/audit/audit.go"
      via: "audit.Write call inside ReverseJournalEntry's caller-owned transaction"
      pattern: "audit\\.Write"
    - from: "server/ledger/period.go"
      to: "server/audit/audit.go"
      via: "audit.Write call inside LockPeriod's transaction"
      pattern: "audit\\.Write"
---

<objective>
Build the immutable audit log (AUTH-05) using the exact append-only trigger pattern Phase 1 already proved for `journal_lines`/`journal_entries`, then wire it into the three existing ledger mutation entry points (post, reverse, lock period) so every create/edit/settings-change action is attributed to a real logged-in user.

Purpose: The core trust guarantee of Phase 2 is that every consequential action is attributable and that record can never be altered after the fact — this plan is where that guarantee becomes real, reusing Phase 1's proven trigger approach rather than inventing a new mechanism.
Output: `audit_log` migration + triggers, `server/audit` write helper, and wiring into `PostJournalEntry`, `ReverseJournalEntry`, and `LockPeriod`.
</objective>

<execution_context>
@/Users/darren/.claude/get-shit-done/workflows/execute-plan.md
@/Users/darren/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-auth-rbac-backup-dr-foundations/02-RESEARCH.md
@.planning/phases/02-auth-rbac-backup-dr-foundations/02-VALIDATION.md
@server/db/migrations/0003_journal.up.sql
@server/ledger/posting.go
@server/ledger/reversal.go
@server/ledger/period.go

<interfaces>
<!-- Existing ledger entry points this plan modifies. Actor IDs are ALREADY threaded through as plain params — no context plumbing needed. -->

From server/ledger/posting.go (02-01/Phase 1, unchanged signature):
```go
type NewEntry struct { EntryDate, Memo string; PostedBy int64; Source string; Lines []NewLine }
func PostJournalEntry(db *sql.DB, e NewEntry) (int64, error) // manages its own tx internally
```

From server/ledger/reversal.go (Phase 1, unchanged signature):
```go
func ReverseJournalEntry(tx *sql.Tx, originalID int64, reason string, postedBy int64) (int64, error) // caller owns the tx
```

From server/ledger/period.go (Phase 1):
```go
func LockPeriod(db *sql.DB, throughDate string, lockedBy int64) error // currently uses db.Exec, no tx — this plan wraps it in a tx to combine with the audit insert atomically
```

From server/db/migrations/0003_journal.up.sql (append-only trigger pattern to mirror exactly):
```sql
CREATE TRIGGER journal_entries_no_update BEFORE UPDATE ON journal_entries
BEGIN SELECT RAISE(ABORT, '...immutable...'); END;
-- paired BEFORE DELETE trigger, same RAISE(ABORT) pattern
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: audit_log schema, append-only triggers, and Write helper</name>
  <files>server/db/migrations/0007_audit_log.up.sql, server/db/migrations/0007_audit_log.down.sql, server/audit/audit.go, server/audit/audit_test.go</files>
  <behavior>
    - TestAuditImmutable: inserting a row into audit_log succeeds; attempting `UPDATE audit_log SET ... WHERE id = ?` fails with the trigger's RAISE(ABORT) error; attempting `DELETE FROM audit_log WHERE id = ?` likewise fails. Both are rejected at the database layer even via a raw `db.Exec`, not just through the Go helper.
  </behavior>
  <action>
    Create migration 0007_audit_log mirroring `server/db/migrations/0003_journal.up.sql`'s immutability pattern exactly: `audit_log(id INTEGER PRIMARY KEY, actor_user_id INTEGER NOT NULL REFERENCES users(id), action TEXT NOT NULL, entity_type TEXT NOT NULL, entity_id INTEGER, detail TEXT, occurred_at TEXT NOT NULL DEFAULT (datetime('now')))`, an index on `(entity_type, entity_id)`, and paired `BEFORE UPDATE`/`BEFORE DELETE` triggers that `RAISE(ABORT, 'audit_log is append-only')`. This migration depends on `users` (0005) already existing — do not merge the FK unless migration ordering guarantees `users` runs first (it does, 0005 < 0007).

    Implement `server/audit/audit.go`: `Write(tx *sql.Tx, actorUserID int64, action, entityType string, entityID int64, detail string) error` — inserts one row using the caller's transaction (never opens its own tx; the whole point is atomicity with the mutating operation it's recording). `action` should be one of `"create"`, `"reversal"`, `"lock_period"` (extend as needed) — validate non-empty, don't over-engineer an enum.
  </action>
  <verify>
    <automated>go test ./server/audit/... -run TestAuditImmutable -x</automated>
  </verify>
  <done>Migration applies cleanly; UPDATE and DELETE on audit_log are both rejected by triggers; Write() successfully inserts within a caller-supplied tx; TestAuditImmutable green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire audit attribution into posting, reversal, and period lock</name>
  <files>server/ledger/posting.go, server/ledger/reversal.go, server/ledger/period.go, server/audit/audit_test.go</files>
  <behavior>
    - TestAuditAttribution: calling `PostJournalEntry` creates exactly one `audit_log` row with `action="create"`, `entity_type="journal_entry"`, `entity_id` matching the new entry ID, and `actor_user_id` equal to `NewEntry.PostedBy`. Calling `ReverseJournalEntry` creates one `audit_log` row with `action="reversal"`, `entity_id` matching the new reversal entry ID, `actor_user_id` equal to the `postedBy` argument. Calling `LockPeriod` creates one `audit_log` row with `action="lock_period"`, `actor_user_id` equal to `lockedBy`. In every case the actor ID comes from the function's existing parameter (already server-side-controlled), never from any new client-supplied field.
  </behavior>
  <action>
    Modify `PostJournalEntry` (server/ledger/posting.go): after inserting `journal_entries`/`journal_lines` and before `tx.Commit()`, call `audit.Write(tx, e.PostedBy, "create", "journal_entry", entryID, e.Memo)`.

    Modify `ReverseJournalEntry` (server/ledger/reversal.go): after inserting the reversal entry/lines, call `audit.Write(tx, postedBy, "reversal", "journal_entry", newID, memo)` — this function already receives the caller's tx, no transaction restructuring needed.

    Modify `LockPeriod` (server/ledger/period.go): change it to open its own transaction (`db.Begin()` / `defer tx.Rollback()`), perform the existing `INSERT ... ON CONFLICT` via `tx.Exec`, call `audit.Write(tx, lockedBy, "lock_period", "accounting_period", 1, throughDate)`, then `tx.Commit()`. This makes the settings change and its audit record atomic, matching the pattern in the other two functions.

    Do not add a foreign-key backfill migration for Phase 1's existing `journal_entries.posted_by`/`accounting_periods.locked_by` columns — those remain plain integers as Phase 1 left them (avoids disrupting existing Phase 1 test fixtures); only the new `audit_log.actor_user_id` column gets the `REFERENCES users(id)` FK, which is satisfied going forward since Phase 2 plan 02-01 requires all actors to be real authenticated users from this point on.
  </action>
  <verify>
    <automated>go test ./server/audit/... -run TestAuditAttribution -x</automated>
  </verify>
  <done>PostJournalEntry, ReverseJournalEntry, and LockPeriod each write a correctly-attributed audit_log row in the same transaction as their primary write; TestAuditAttribution green; existing Phase 1 ledger tests (`go test ./server/ledger/...`) remain green.</done>
</task>

</tasks>

<verification>
`go test ./server/audit/... ./server/ledger/... -x` passes in full; `go test ./...` (full suite) remains green, confirming Phase 1 ledger behavior is unaffected by the audit wiring.
</verification>

<success_criteria>
AUTH-05 (every create/edit/delete action attributed to the logged-in user in an immutable audit log) is fully implemented and test-covered for the three existing ledger mutation paths, using database-enforced immutability rather than application-layer convention.
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth-rbac-backup-dr-foundations/02-03-SUMMARY.md`
</output>
